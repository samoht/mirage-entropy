(library
  (name        mirage_entropy)
  (public_name mirage-entropy)
  (libraries   lwt cstruct mirage-scheduler)
  (c_names     entropy_cpu_stubs)
  (c_flags     :standard (:include cflags.sexp) -std=c99 -Wall -Wpedantic)

)

(rule (with-stdout-to cflags.sexp (run ./cflags.sh)))
